<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >
    
    <application
        android:name=".main.RealEstateManagerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.RealEstateManager"
        tools:targetApi="31"
        >
        <provider
            android:name="com.despaircorp.data.estate.content_provider.EstateContentProvider"
            android:authorities="com.despaircorp.data.estate.content_provider"
            android:exported="true"
            />
    </application>

</manifest>